diff --git a/dist/bridge-handlers.js b/dist/bridge-handlers.js
index 69fb402611ba55b1df6b636857350538ba54a553..fb93de2d55d3a306a46ae226d76d3bf427c48b76 100644
--- a/dist/bridge-handlers.js
+++ b/dist/bridge-handlers.js
@@ -2215,8 +2215,6 @@ export function buildModuleResolutionBridgeHandlers(deps) {
     // CJS transforms when require() needs ESM or import() support.
     handlers[K.loadFileSync] = (filePath) => {
         const sandboxPath = String(filePath);
-        if (sandboxPath.includes("balanced"))
-            console.error(`[loadFileSync] path=${sandboxPath}`);
         const hostPath = deps.sandboxToHostPath(sandboxPath) ?? sandboxPath;
         return loadHostModuleSourceSync(hostPath, sandboxPath, "require");
     };
diff --git a/dist/module-source.js b/dist/module-source.js
index e1b2c8371175d672eda27e9d312c1ff73acb141c..ec8d9c49d14498ad47ccd22786af93fd778356d1 100644
--- a/dist/module-source.js
+++ b/dist/module-source.js
@@ -115,9 +115,6 @@ function isCommonJsModuleForImportSync(source, formatPath) {
     }
     if (formatPath.endsWith(".js")) {
         const packageType = getNearestPackageTypeSync(formatPath);
-        if (formatPath.includes("balanced")) {
-            console.error(`[isCommonJsModuleForImportSync] path=${formatPath} packageType=${packageType}`);
-        }
         if (packageType === "module") {
             return false;
         }
@@ -126,9 +123,6 @@ function isCommonJsModuleForImportSync(source, formatPath) {
         }
         initSync();
         const syntax = parseSourceSyntax(source, formatPath);
-        if (formatPath.includes("balanced")) {
-            console.error(`[isCommonJsModuleForImportSync] hasModuleSyntax=${syntax.hasModuleSyntax}`);
-        }
         return !syntax.hasModuleSyntax;
     }
     return false;
diff --git a/dist/execution-driver.js b/dist/execution-driver.js
index 642e18c0adbb7d9d19ccddaa9b908ca33e9ebcf4..7b570ad753ef0969037f0c6f664e34039a2c35a0 100644
--- a/dist/execution-driver.js
+++ b/dist/execution-driver.js
@@ -1403,9 +1403,6 @@ function getPatchedRequireSetupCode() {
     if (source.includes('if (name === "sqlite") {')) {
         return source;
     }
-    source = source.replace('  function _requireFrom(moduleName, fromDir) {\n    _debugRequire("start", moduleName, fromDir);', '  function _requireFrom(moduleName, fromDir) {\n    if (typeof console !== "undefined" && typeof console.error === "function") {\n      console.error("[sandbox.require] start " + moduleName + " " + fromDir);\n    }\n    _debugRequire("start", moduleName, fromDir);');
-    source = source.replace('    resolved = _resolveFrom(name, fromDir);\n    cacheKey = resolved;', '    resolved = _resolveFrom(name, fromDir);\n    if (typeof console !== "undefined" && typeof console.error === "function") {\n      console.error("[sandbox.require] resolved " + name + " -> " + resolved);\n    }\n    cacheKey = resolved;');
-    source = source.replace('      wrapper(\n        module.exports,', '      if (typeof console !== "undefined" && typeof console.error === "function") {\n        console.error("[sandbox.require] execute " + resolved);\n      }\n      wrapper(\n        module.exports,');
     const sqliteBuiltinSource = `
     if (name === "sqlite") {
       if (__internalModuleCache["sqlite"]) return __internalModuleCache["sqlite"];
